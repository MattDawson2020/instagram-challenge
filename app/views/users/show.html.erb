

<%= "#{@user.name}'s profile page" %>
<br>

<% if @user.image.attached? %>
  <%= image_tag @user.image, style: "display: block" %>
<% end %>

<div>
  <%= link_to "Edit user", edit_user_path(@user) %>
</div>
<br>
<div>
  <%= link_to "Create a new Jam", new_jam_path(@user) %>
</div>

