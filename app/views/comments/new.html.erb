
<div>
  <%= form_for @comment do |f| %>
    <%= f.label :body, "Comment" %>
    <%= f.text_field :body %> 
    <%= f.hidden_field :jam_id, value: @jam.id %>
    <%= f.hidden_field :user_id, value: @user.id %>

    <%= f.submit "Submit" %>
  <% end %>
</div>
